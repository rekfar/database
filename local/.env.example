# Copy to local/.env and set a password. Local development only — local/.env is
# gitignored, and this value must never be reused for anything that matters.
#
# SQL Server enforces its own complexity rules: at least 8 characters from three of
# upper case, lower case, digits and symbols, or the container exits on first start.
MSSQL_SA_PASSWORD=Change-me-locally-1
